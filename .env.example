BOT_TOKEN=123456:replace-with-botfather-token
ALLOWED_CHAT_ID=000000000
POLL_TIMEOUT=50

# /usage quota % requires `cswap` (Multi-Account Switcher for Claude Code)
# installed on PATH — no config needed here.
